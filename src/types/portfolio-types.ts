export interface IProject {
  title: string;
  description: string;
  link?: string;
}

export interface IExperience {
  company: string;
  companySite: string;
  position: string;
  date: string;
  description: string;
  technologies: string[];
}
